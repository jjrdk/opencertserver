# OpenCertServer Build

Builds OpenCertServer, a server for issuing and verifying OpenCerts certificates.

This build script automates the process of compiling and packaging OpenCertServer, 
ensuring that it can be easily deployed and run on various platforms. 
It includes tasks for cleaning up build artifacts, compiling the application, and generating release packages.
